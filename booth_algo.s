	AREA DATA1, DATA; blah
A EQU 0X0000
q EQU 0x0000 ;Q-1 bit
M EQU 0x0101
Q EQU 0X1010
N EQU 0X0004
w EQU 0x00; ASR
x EQU 0x11; ASR
y EQU 0x10; A<--A-M
z EQU 0x01; A<--A+M	
	AREA P01,CODE
			ENTRY 
	EXPORT __START		
__START
	MOV R1,A; Accumulator
	MOV R2,Q; Multiplier
	MOV R3,q; Booth Bit
	MOV R4, M; Mulitplicand
LABEL B LABEL		
	END